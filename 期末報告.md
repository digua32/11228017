# 訊號與系統 期末報告：RC 低通濾波器之雜訊濾除分析

本專案為「訊號與系統」課程之期末報告，旨在透過拉普拉斯變換（Laplace Transform）分析 RC 低通濾波器對高頻雜訊的濾除效果。

> **備註**：本報告之問題背景與參數設定完整引用自說明文件 `1000016824.jpg`。

---

## 五、問題描述（信號與系統分析）

在信號採集電路中，常用 RC 低通濾波器濾除信號中的高頻雜訊。已知 RC 低通濾波器的參數如下：
* **電阻**：$R = 1\text{ k}\Omega$
* **電容**：$C = 1\ \mu\text{F}$

輸入訊號為帶有脈衝雜訊的因果信號：
$$f(t) = [\sin(10t) + 0.5\sin(1000t)]u(t)$$

其中：
* $\sin(10t)$ 是**目標低頻信號**
* $0.5\sin(1000t)$ 是**高頻雜訊**
* $u(t)$ 是**單位階躍函數（Unit Step Function）**
* **初始狀態**：電容未充電（即零狀態響應，Zero-State Response）

### 求解目標
利用**拉普拉斯變換**求出輸出電壓 $y(t)$，並驗證該濾波器對高頻雜訊的濾除效果。

---

## 六、數學推導與分析

### 1. 系統轉移函數 (Transfer Function)
RC 低通濾波器的頻域轉移函數 $H(s)$ 定義為：
$$H(s) = \frac{1}{RCs + 1}$$

代入已知參數 $R = 1\times 10^3\ \Omega$ 與 $C = 1\times 10^{-6}\text{ F}$：
$$RC = 10^3 \times 10^{-6} = 10^{-3}\text{ 秒} = \frac{1}{1000}$$

因此，系統轉移函數為：
$$H(s) = \frac{1}{\frac{1}{1000}s + 1} = \frac{1000}{s + 1000}$$

### 2. 輸入訊號的拉普拉斯變換 $F(s)$
根據拉普拉斯變換公式 $\mathcal{L}\{\sin(\omega t)u(t)\} = \frac{\omega}{s^2 + \omega^2}$，將輸入訊號 $f(t)$ 轉換至 $s$ 域：
$$F(s) = \frac{10}{s^2 + 10^2} + 0.5 \cdot \frac{1000}{s^2 + 1000^2} = \frac{10}{s^2 + 100} + \frac{500}{s^2 + 1000000}$$

### 3. 輸出訊號的 $s$ 域表達式 $Y(s)$
輸出訊號 $Y(s) = F(s) \cdot H(s)$，展開後可得：
$$Y(s) = \underbrace{\frac{10000}{(s + 1000)(s^2 + 100)}}_{Y_{\text{signal}}(s)} + \underbrace{\frac{500000}{(s + 1000)(s^2 + 1000000)}}_{Y_{\text{noise}}(s)}$$

透過**部分分式展開法（Partial Fraction Expansion）**並求其**逆拉普拉斯變換（Inverse Laplace Transform）**，即可求得時間域的輸出電壓 $y(t)$。（此處可依據您的計算補上具體係數）

---

## 七、模擬與驗證 (Python Simulation)

為了驗證上述數學推導並直觀觀察濾波效果，本專案使用 Python 的 `scipy.signal` 模組進行時域模擬。

### 執行環境
```bash
pip install numpy scipy matplotlib
