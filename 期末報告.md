# 期末報告：

## 五、問題描述（信號與系統分析）

在信號採集電路中，常用 RC 低通濾波器濾除信號中的高頻雜訊。已知 RC 低通濾波器的參數如下：
* **電阻**：$R = 1\text{ k}\Omega$
* **電容**：$C = 1\ \mu\text{F}$

輸入訊號為帶有脈衝雜訊的因果信號：
$$f(t) = [\sin(10t) + 0.5\sin(1000t)]u(t)$$

其中：
* $\sin(10t)$ 是**目標低頻信號**
* $0.5\sin(1000t)$ 是**高頻雜訊**
* $u(t)$ 是**單位階躍函數（Unit Step Function）**
* **初始狀態**：電容未充電（即零狀態響應，Zero-State Response）

### 求解目標
利用**拉普拉斯變換**求出輸出電壓 $y(t)$，並驗證該濾波器對高頻雜訊的濾除效果。

---

## 六、數學推導與分析

### 1. 系統轉移函數 (Transfer Function)
RC 低通濾波器的頻域轉移函數 $H(s)$ 定義為：
$$H(s) = \frac{1}{RCs + 1}$$

代入已知參數 $R = 1 \times 10^3\ \Omega$ 與 $C = 1 \times 10^{-6}\text{ F}$：
$$RC = 10^3 \times 10^{-6} = 10^{-3}\text{ 秒} = \frac{1}{1000}$$

因此，系統轉移函數為：
$$H(s) = \frac{1}{\frac{1}{1000}s + 1} = \frac{1000}{s + 1000}$$

### 2. 輸入訊號的拉普拉斯變換 $F(s)$
根據拉普拉斯變換公式：
$$\mathcal{L}\{\sin(\omega t)u(t)\} = \frac{\omega}{s^2 + \omega^2}$$

將輸入訊號 $f(t)$ 轉換至 $s$ 域：
$$F(s) = \mathcal{L}\{\sin(10t)u(t)\} + 0.5 \cdot \mathcal{L}\{\sin(1000t)u(t)\}$$

$$F(s) = \frac{10}{s^2 + 10^2} + 0.5 \cdot \frac{1000}{s^2 + 1000^2} = \frac{10}{s^2 + 100} + \frac{500}{s^2 + 1000000}$$

### 3. 輸出訊息的 $s$ 域表達式 $Y(s)$
輸出訊號的 $s$ 域表達式為 $Y(s) = F(s) \cdot H(s)$，展開後可分為目標信號部分與雜訊部分：

$$Y(s) = Y_{\text{signal}}(s) + Y_{\text{noise}}(s)$$

$$Y(s) = \frac{10000}{(s + 1000)(s^2 + 100)} + \frac{500000}{(s + 1000)(s^2 + 1000000)}$$

### 4. 部分分式展開法 (Partial Fraction Expansion) 與逆變換

為了求得時間域的輸出電壓 $y(t)$，我們對 $Y_{\text{signal}}(s)$ 與 $Y_{\text{noise}}(s)$ 分別進行部分分式展開：

#### (1) 目標信號部分 $Y_{\text{signal}}(s)$ 展開：
設 $\frac{10000}{(s + 1000)(s^2 + 100)} = \frac{A}{s + 1000} + \frac{Bs + C}{s^2 + 100}$
經過代數求解可得精確係數：
* $A = \frac{10000}{1000^2 + 100} \approx 0.01$
* $B = -0.01$
* $C = 10$

$$Y_{\text{signal}}(s) \approx \frac{0.01}{s + 1000} - \frac{0.01s}{s^2 + 100} + \frac{10}{s^2 + 100}$$

進行逆拉普拉斯變換：
$$y_{\text{signal}}(t) = [0.01e^{-1000t} - 0.01\cos(10t) + \sin(10t)]u(t)$$

#### (2) 雜訊部分 $Y_{\text{noise}}(s)$ 展開：
設 $\frac{500000}{(s + 1000)(s^2 + 1000000)} = \frac{D}{s + 1000} + \frac{Es + F}{s^2 + 1000000}$
經過代數求解可得精確係數：
* $D = \frac{500000}{1000^2 + 1000000} = 0.25$
* $E = -0.25$
* $F = 250$

$$Y_{\text{noise}}(s) = \frac{0.25}{s + 1000} - \frac{0.25s}{s^2 + 1000000} + \frac{250}{s^2 + 1000000}$$

進行逆拉普拉斯變換（注意：$\frac{250}{s^2+1000^2} = 0.25 \cdot \frac{1000}{s^2+1000^2}$）：
$$y_{\text{noise}}(t) = [0.25e^{-1000t} - 0.25\cos(1000t) + 0.25\sin(1000t)]u(t)$$

#### (3) 完整時域輸出電壓 $y(t)$：
將兩部分合併，最終得到輸出電壓的完整解析解：
$$y(t) = y_{\text{signal}}(t) + y_{\text{noise}}(t)$$

$$y(t) = \left[ 0.26e^{-1000t} + \sin(10t) - 0.01\cos(10t) + 0.25\sin(1000t) - 0.25\cos(1000t) \right]u(t)$$

---

## 七、模擬與驗證（Python 模擬）

為了驗證上述數學推導並直觀觀察濾波效果，本專案使用 Python 的 `scipy.signal` 模組進行時域模擬。

### 執行環境準備
```bash
pip install numpy scipy matplotlib
